---
name: reference-key-rotation
description: How to rotate the staging API keys.
metadata:
  type: reference
---

Rotate the staging API keys with `scripts/rotate-keys.sh` from the repo root. Takes about a minute; the old keys stay valid for an hour after rotation.
